# Handoff — 90e76764-46b3-49aa-8786-873d47a529fd
2026-09-15 17:01 BST · dinner-app (MealPlanner iOS app) / branch `master`

## Unfinished tasks
1. [pending] Review the M8 commit (`19e349e M8: Randomiser`). It was pushed by Sonnet after this session's last review. Same review routine as before (see Context). Pay particular attention to: randomise **Replace All** must pass `dependents: .remove` to `WeekPlanService.assign` (SPEC §8.2 step 3), so leftovers of replaced meals are deleted, not kept as cooked; last week's meals are never picked; single-slot Shuffle (the "Randomise Dinners → Shuffle Tuesday only" flow in §10.1); locked leftover slots; no database fetches repeated per row or per redraw (a recurring issue).
2. [pending] Remaining milestones, one review each: M9 shopping list and hand-added items, M10 export/WhatsApp, M11 notifications/settings, M12 polish/QA. Post-MVP §18 (M13–M15, recipe photo import) only when the user explicitly asks.

## Context

**Goal.** The user (Caolan, UK) is building a SwiftUI + SwiftData iPhone meal-planning app. I (Opus) wrote the spec. **Sonnet implements** one milestone per session and pushes. I **review each commit**: read the diff, build/test, drive the iOS Simulator, and hand the user a list of findings plus a ready-to-paste Sonnet fix prompt. The user relays prompts; I don't edit app code myself.

**Done this session.**
- `SPEC.md` (now v1.3, ~2,300 lines, source of truth) and `CLAUDE.md` (Sonnet's rules) written and iterated: frozen past weeks, ingredient library (shopping list merges by ingredient ID), hand-added shopping items, leftovers, randomiser excluding last week, meal photos, WhatsApp via `wa.me` (including a saved contact, so the list reaches the user's Android partner), post-MVP §18 photo import through the Claude API (M13–M15, with JSON schema, prompts and checklists) plus §18.11 notes on a public/paid release, and v1.3 "Good as Leftovers" (new milestone M7.1).
- `CLAUDE.md` now freezes `SchemaV1`: every future `@Model` change needs `SchemaV2` or later, a migration stage and an on-disk migration test (commit `a52e898`).
- Reviewed M1–M7.1 plus each round of fixes; all merged. Answered the user's questions on API cost, subscriptions/tax, market potential and pack sizes (eggs in packs of 12: current behaviour kept by the user).

**In progress / next.** Start task 1: `git fetch`, `git show 19e349e --stat`, read the diff (DECISIONS.md first), run the tests, build, then check it in the Simulator.

**Key files & paths.**
- `SPEC.md`: §16 milestones and acceptance criteria; §7.6 randomiser algorithm; §8.2 `WeekPlanService`; §10.1 Plan screen and randomising UI.
- `CLAUDE.md`, `DECISIONS.md` (Sonnet's deviation log; read it first on every review).
- App code is in `MealPlanner/MealPlanner/{App,Models,Domain,Services,Features,Shared}`, tests in `MealPlanner/MealPlannerTests`.
- `Shared/AppRoute.swift`: all navigation is value-based, with destinations declared once per tab.
- `Services/WeekPlanService.swift` and its `+Leftovers` / `+Randomize` extensions; `Domain/LeftoverRules.swift` (holds `PlanWeekContext` and `slotMenuFlags`).

**Decisions & gotchas.**
- **Review output format:** a verified-in-Simulator table, a numbered list of issues with file:line links, then a fenced Sonnet prompt starting "Read CLAUDE.md. Fix these M<N> findings…, commit as "M<N> fixes: …", and push." Keep it plain English for the user.
- **Recurring bug types found so far:**
  - ModelContainer freed while its ModelContext is still in use (tests);
  - mixing view-based and value-based navigation (taps do nothing);
  - a Swift 6 trailing closure binding to the wrong optional closure parameter;
  - per-row database fetches during view redraws;
  - leftover chains (leftovers pointing at leftovers).
- **Simulator:** iPhone 17 Pro, UDID `3ED3D8B9-DC5A-4007-818D-71E5E2E7CEE0` (iOS 26.5). Boot it with `xcrun simctl boot`.
  - Build into scratch derived data: `-derivedDataPath <scratchpad>/dd-app`.
  - Launch with the `mcp__Claude_Code_iOS_Simulator__control` tool (`launch` with `app_path` …/Debug-iphonesimulator/MealPlanner.app).
  - The tool's `screenshot` often fails with `captureFailed`. Use `xcrun simctl io <UDID> screenshot <file>` then Read, with `sips -Z 700` to keep images small.
  - `inspect` is unavailable.
  - **Tapping a SwiftUI Toggle doesn't flip it via the tool; do a short swipe across the switch instead.**
  - Long-press is `tap` with `duration` 1.2.
  - The screen is 402×874 points; screenshot pixels ÷ ~2.29 = points.
- **Simulator data:** existing test data (example meals, leftovers, photos) lives only there. Example meals saved before M7.1 have "Good as Leftovers" on (I turned Scrambled eggs off during testing).
- **Sonnet's working tree:** make sure my own SPEC/CLAUDE edits are committed before the user's next Sonnet session (they were, as of `a52e898`).
- **Pending M8 note:** `assign`'s `dependents` defaults to `.keepAsCooked`; randomise Replace All must pass `.remove`.

**How to verify.** From `MealPlanner/`:
`xcodebuild -project MealPlanner.xcodeproj -scheme MealPlanner -destination "id=3ED3D8B9-DC5A-4007-818D-71E5E2E7CEE0" test 2>&1 | grep -E "\*\* TEST|error: |warning: " | grep -v appintents`
(expect `** TEST SUCCEEDED **` and no warnings).
